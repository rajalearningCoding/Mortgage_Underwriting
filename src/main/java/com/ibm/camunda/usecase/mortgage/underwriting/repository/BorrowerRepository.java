/**
 * 
 */
package com.ibm.camunda.usecase.mortgage.underwriting.repository;
/*import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
/**
 * @author LIYAJERARD
 *
 */

import com.ibm.camunda.usecase.mortgage.underwriting.model.Personal;

/**
	 * 
	 */
	import java.util.List;

/*public interface BorrowerRepository extends JpaRepository<Personal, Integer> {

	    List<Personal> findByPan(String pan);

	   /* @Query("SELECT C FROM Company C WHERE C.contactPerson.firstName = ?1")
	    List<Company> findByContactPersonFirstNameWithJPQL(String firstName);
*/
	   /* @Query(value = "SELECT * FROM \"Personal\" WHERE \"pan\" = ?1", nativeQuery = true)
	    List<Personal> findByPan(String pan	);
	}

}*/
